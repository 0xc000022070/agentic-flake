name: Sync unofficial skills

on:
  schedule:
    - cron: "0 0,13 * * *"
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Setup Nix
        uses: cachix/install-nix-action@v27

      - name: Configure Git
        run: |
          git config user.name "skills-updater[bot]"
          git config user.email "skills-updater[bot]@users.noreply.github.com"

      - name: Sync providers
        run: nix develop --command bun .github/scripts/sync-unofficial-skills.ts

      - name: Commit
        run: |
          if ! git diff --quiet sources.json; then
            git add sources.json
            git commit -m "chore: sync unofficial providers"
            git push
          fi
